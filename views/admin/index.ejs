<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>

  <section style="display: flex; flex-direction: column; align-items: center;">

    <p style="font-size: 20px;">LISTA PIESE</p>

    <% vectorPieseFinalizate.forEach(rtsp => { %>
      <div style="width: 85%; display: flex; justify-content: center; border-radius: 1em; border: 2px solid black; background-color: gray;">
        <%= rtsp.Numar_comanda %> <%= rtsp.cantitateaTotala %> <%= rtsp.cantitateRebut %> <br>
        <%= rtsp.dataInitiere %> <%= rtsp.dataFinalizare %> <br>
        <%= rtsp.rezumatOperatiiFinalizate %> <br>
        <%= rtsp.timpDeExecutieTotal %>
        <form method="POST" action="./sterge-rtsp">
          <input type="hidden" id="id" name="id" value="<%= rtsp.RTSP %>">
          <button type="submit">Sterge Profil</button>
        </form>
      </div>
    <% }); %>

  </section>

</body>
</html>